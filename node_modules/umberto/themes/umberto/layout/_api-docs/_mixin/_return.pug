include ./_type.pug
include ./_params.pug

mixin return( ret )
    dt
        code
            if ret.types
                +type( ret.types )
    dd
        if ( ret.description )
            | !{ ret.description.content }

        if isNonEmptyArray( ret.params )
            +paramsMixin( ret.params, 5, 'Properties' )