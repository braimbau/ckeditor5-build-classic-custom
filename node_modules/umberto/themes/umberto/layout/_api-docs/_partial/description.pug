include ../_mixin/_api-see-source.pug

if ( data.description )
	div( class="main-description__outer hidden-loading" )

			+seeSource( data )

			div( class="main-description")
				| !{ data.description.content }
