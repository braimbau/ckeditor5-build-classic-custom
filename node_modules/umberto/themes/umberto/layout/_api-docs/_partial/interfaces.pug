include ../_mixin/_class-item

if isNonEmptyArray( data.interfaces )
	h2( id="interfaces" class="hidden-loading" ) Interfaces
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" )
		each i in data.interfaces
			+classItem( i )
