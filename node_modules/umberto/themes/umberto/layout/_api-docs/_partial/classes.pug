include ../_mixin/_class-item

if isNonEmptyArray( data.classes )
	h2( id="classes" class="hidden-loading" ) Classes
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" )
		each c in data.classes
			+classItem( c )
