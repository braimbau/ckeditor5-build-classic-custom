include ../_mixin/_method

if isNonEmptyArray( data.events )
	h2( id="events" class="hidden-loading" ) Events
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" data-type="events" )
		each e in data.events
			+method( e )
