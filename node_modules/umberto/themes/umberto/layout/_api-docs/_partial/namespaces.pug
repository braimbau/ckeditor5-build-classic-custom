include ../_mixin/_class-item

if isNonEmptyArray( data.namespaces )
	h2( id="namespaces" class="hidden-loading" ) Namespaces
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" )
		each i in data.namespaces
			+classItem( i )
