include ../_mixin/_property

if isNonEmptyArray( data.configOptions )
	h2( id="config-options" class="hidden-loading" ) Config options
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" data-type="configs" )
		each c in data.configOptions
			+property( c )
