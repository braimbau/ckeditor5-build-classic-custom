include ../_mixin/_property

if isNonEmptyArray( data.constants )
	h2( id="constants" class="hidden-loading" ) Constants
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" )
		each m in data.constants
			+property( m )
