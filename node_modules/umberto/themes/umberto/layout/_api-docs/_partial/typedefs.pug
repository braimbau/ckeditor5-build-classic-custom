include ../_mixin/_class-item

if isNonEmptyArray( data.typedefs )
	h2( id="type-definitions" class="hidden-loading" ) Type Definitions
	ul( class="collapsing-list collapsing-list--filtered hidden-loading" )
		each t in data.typedefs
			+classItem( t )
