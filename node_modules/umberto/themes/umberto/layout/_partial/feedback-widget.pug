//- 1. Check if property exists in umberto-main.json.
//- 2. Do not render feedback-widget if disabled via single .md file.
if ( feedbackWidget && page[ 'feedback-widget' ] !== false )

	//- Render feedback-widget only for domains configured in umberto-main.json.
	script.
		if ( !{ JSON.stringify( feedbackWidget.domains ) }.indexOf( location.hostname ) !== -1) {
			window.emojicsOpts = {
				widget: '!{ feedbackWidget.apiKey }',
				position: 'inline'
			};
			( function( d, s, id ) {
				var js, fjs = d.getElementsByTagName( s )[ 0 ];
				js = d.createElement( s );
				js.id = id;
				js.async = 1;
				js.src = '//connect.emojics.com/dist/sdk.js';
				fjs.parentNode.insertBefore( js, fjs );
			} )( document, 'script', 'emojics-js' );
		}
